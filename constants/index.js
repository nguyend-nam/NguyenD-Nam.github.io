import { keyframes } from "styled-components";
export const theme = {
  colors: {
    primary: "#57C5B6",
    secondary: "#FFF",
    darkBlue: "#223",
    shadow: "#F7DB6A",
  },
};
export const appear = keyframes`
from {
  opacity: 0;
}

to {
  opacity: 1;
}
`;
