import { keyframes } from "styled-components";
export const theme = {
  colors: {
    primary: "#64CCC5",
    secondary: "#F2F7FF",
    darkBlue: "#223",
    shadow: "#FEC260",
  },
};
export const appear = keyframes`
from {
  opacity: 0;
}

to {
  opacity: 1;
}
`;
