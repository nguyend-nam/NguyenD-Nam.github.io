import { keyframes } from "styled-components";
export const theme = {
  colors: {
    primary: "#242F9B",
    secondary: "#DBDFFD",
    shadow: "#646FD4",
    shadowLight: "#9BA3EB",
  },
};
export const appear = keyframes`
from {
  opacity: 0;
}

to {
  opacity: 1;
}
`;
